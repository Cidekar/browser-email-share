/**
 * The entry file will only contain exports export { <method> } from '/location/at/disk'
 * Use to remap for client sake of having to call Module.Module; now they are able to say 
 * something like Module.API.Method; 
 */

export { default } from '../index'